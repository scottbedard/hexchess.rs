use crate::hexchess::hexchess::Hexchess;

use crate::constants::{
    Color,
    San,
};

pub fn knight_moves_unsafe(
    _hexchess: &Hexchess,
    _from: &u8,
    _color: &Color,
) -> Vec<San> {
    let result: Vec<San> = vec![];

    result
}
